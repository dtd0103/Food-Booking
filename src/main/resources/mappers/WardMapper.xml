<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dtdat.javaweb.exam.mapper.WardMapper">
	<select id="getByCityId" resultType="com.dtdat.javaweb.exam.entity.Ward">
		SELECT
		id,
		name,
		city_id as cityId,
		shipping_fee as shippingFee
		FROM ward where city_id = #{cityId}
	</select>
	
	<select id="getById" resultType="com.dtdat.javaweb.exam.entity.Ward">
		SELECT
		id,
		name,
		city_id as cityId,
		shipping_fee as shippingFee
		FROM ward where id = #{id}
	</select>
</mapper>